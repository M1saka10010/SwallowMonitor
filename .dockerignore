# Build artifacts
swallow-monitor
swallow-monitor.exe
swallow-monitor.exe~
*.exe
*.test

# Runtime data
*.db
*.db-shm
*.db-wal
*.sqlite
*.sqlite3

# Local config with secrets
config.yaml
config.local.yaml
*.local.yaml

# VCS / editor / OS
.git
.gitignore
.idea
.vscode
*.log
.DS_Store
Thumbs.db

# Frontend local dependencies and reports
frontend/node_modules
frontend/dist
frontend/coverage
web/static

# Docker metadata is available to the builder, but is not copied into images.
docker-compose.yml
.dockerignore
