# DESIGN.md — SwallowMonitor

## 1. Objective

SwallowMonitor 应该像一页装帧考究的账本：纸一样安静的底子、墨一样精确的数字，主机状态一行一行被登记得清清楚楚。美感来自极致的黑白灰克制、细线与字号的反差，而不是任何颜色或装饰；密度来自表格化排版，但任何一屏如果"密"与"美"冲突，删信息、保美感。质量标准：这是一件值得截图放进作品集的监控仪器，同时仍然允许用户在凌晨三点半睡眼惺忪时两秒内确认哪台机器挂了。

## 2. Product Context

- **产品作用：** 在一个 Go 单二进制中展示主机实时状态、资源趋势和管理配置。
- **主要用户：** 同时管理少量到数十台服务器、白天在终端里工作、夜里可能被告警叫醒的个人运维者或小团队；他们对"后台管理模板"有审美疲劳，对精确的数字有本能的信任。
- **相邻参考：** Vercel Dashboard 的单色结构、Financial Times 数据版的细线分区、Teenage Engineering 面板的物理质感（仅质感，不用其彩色）。
- **远离参考：** Grafana 默认仪表盘——多色面板与高密度插件感服务于大型观测平台，与本产品"一页账本"的聚焦感相反。
- **文化语域：** 技术、精确、安静，带一点印刷品的书卷气；不拟人化，不营销化，不赛博。

## 3. Visual Foundations

### 3a. Color

整套系统是**消色的（achromatic）**：结构、层级、交互全部由墨阶承担；颜色只在"状态语义"这一件事上出现，且面积被压到最小（状态点 ≤8px、校准线 2px、状态词、错误消息、危险按钮）。

- **纸主题（light）中性阶：** canvas `#F7F7F5`、surface `#FFFFFF`、surface-muted `#F0F0ED`、text `#141413`、text-muted `#5C5C57`、text-faint `#71716B`、border `#DEDED9`、border-strong `#B4B4AE`。
- **墨主题（dark）中性阶：** canvas `#0C0C0B`、surface `#131312`、surface-muted `#1A1A19`、text `#F2F2EF`、text-muted `#9C9C96`、text-faint `#7F7F79`、border `#262624`、border-strong `#3E3E3B`。
- **强调色：没有色相强调色。** 交互强调是"墨与纸的反转"：主按钮是实心墨块（纸主题上 `#141413` 底 + 纸色文字；墨主题上 `#F2F2EF` 底 + 墨色文字），链接用下划线表示，焦点环用墨色（`--text`）。
- **语义色（唯一允许的颜色，仅用于状态）：** 纸主题 success `#157A4A`、warning `#966600`、danger `#C2372E`、offline `#8F8F89`；墨主题 success `#4EBD85`、warning `#D6A73F`、danger `#E36A60`、offline `#7C7C76`。
- **图表数据色（低饱和色唯一允许出现的地方）：** 蓝 `#3478D4`（主系列）、绿 `#398760`、橙 `#B56A28`、红 `#C64B45`，离线区间 `--chart-offline` 灰虚线；墨主题用对应提亮值（`#79A1EE / #63B58C / #D9A05F / #E0857D`）。系列靠颜色 + 图例区分，主线 1.5、次线 1、三线 0.75。数据色只出现在折线与图例里，永远不进入界面 chrome。
- **使用规则：** 语义色禁止用于背景填充（危险确认按钮是唯一例外）；在线/离线永远同时有状态点和文字；标签、筛选、导航一律中性墨色。不使用任何色相渐变。

### 3b. Typography

- **展示字体（标题）：** 衬线宋体 `"Songti SC", "Noto Serif CJK SC", "Source Han Serif SC", "SimSun", serif`，仅用于站点名、页面 H1、空态标题；字重 600–700，尺寸 ≥20px（衬线在中文小字号下渲染不可靠，这是硬边界）。
- **界面字体（正文与控件）：** `"Helvetica Neue", Arial, "Noto Sans SC", "PingFang SC", "Microsoft YaHei", system-ui, sans-serif`，字重只用 400、500、600。
- **数据字体：** `"SFMono-Regular", "Cascadia Code", "Roboto Mono", Consolas, monospace`，一律 `tabular-nums`；实时数字、表格数值、计数、Token、命令、时间戳全部用它。
- **字号阶梯：** `11 / 12 / 14 / 16 / 20 / 28 / 40`。页头总标题（站点名）20px 衬线，页面 H1 28px 衬线；签名的排版反差：11px 上标微标签（英文大写、字距 `0.12em`；中文用 12px 正常字形）对 28–40px 的大号等宽数字。
- **字重纪律：** 正文 400、控件 500、无衬线标题 600、衬线标题 600–700。禁止在正文里出现 600，禁止用字重代替字号做层级。

### 3c. Spacing & rhythm

- **基础单位：** 4px；阶梯 `4 / 8 / 12 / 16 / 24 / 32 / 48 / 64`。
- **页面框架：** 内容最大宽度 1440px；桌面水平留白 24px、移动端 16px；主要区块间距 24px（比多数后台更紧，密度是承诺的一部分）。
- **密度基准：** 表格/列表行高 44px；账本格（主机卡片）内边距 12–16px、元素间 4px 微距；"慷慨留白"只发生在页面级——区块之间，而不是单元格之内。
- **控件尺寸：** 按钮两档：标准 32px + 13px 字（表单提交、对话框、空态动作）、行内操作 28px + 11px 字（列表行的编辑/删除等）。桌面密集场景不用大按钮；列表行高 ≥44px 承担触达面积。

### 3d. Component seeds

- **按钮：** 四种。primary = 实心墨块反转色；secondary = 透明底 + 1px `border-strong`；ghost = 纯文字、悬停出下划线；danger = 实心 `danger`。圆角 6px，按压时向下位移 1px（实体键程感）。尺寸两档见 §3c。每屏最多一个 primary。
- **分段控件：** 少量互斥小选择（主题切换）用分段控件：细线容器内并排文字按钮，选中项墨纸反转。不用原生 `<select>`——它的平台样式会撕开设计语言。
- **表单控件：** 复选框 14px、4px 圆角、细线边；选中 = 墨纸反转 + 纸色对勾（墨主题反过来）。下拉用自绘 12px 细箭头。不使用平台原生的复选框/下拉渲染——它们又大又响。
- **容器：** 两类容器。主机卡片与图表面板是**独立圆角容器**：10px 圆角、1px 细线边、16px 缝隙（`grid gap-4`），不互相接触；事实带与后台列表是**连续细线带**：一个圆角容器内用 1px 线分行。圆角两档：控件 6px、容器 10px（不超过 12px）——圆角用来柔化边缘，不做大圆角气泡。暗色主题里容器顶部加 1px 内高光 `inset 0 1px 0 rgba(255,255,255,0.04)`，模拟机加工边缘；容器可带 `0 1px 2px rgba(0,0,0,0.04)` 的不可察觉微阴影，只为让它从画布上"呼吸"出来。
- **状态：** 状态点 ≤8px + 文字；概览账本格顶部 2px 状态校准线是产品签名。
- **质感层：** 全局一层内联 SVG `feTurbulence` 噪点纹理，透明度纸主题 2–3%、墨主题 4–5%，`pointer-events: none`——这是唯一的"材质"来源，代替一切图片与装饰。
- **图标：** 不用图标库。动作是文字，方向用文本符号（←、·、/）。
- **凭据区：** Token/安装命令展示区用 1px **虚线**边框——"此处是需要保管的东西"的印刷惯例。

## 4. Accessibility

- 正文对背景 ≥4.5:1，大文本与 UI 边界 ≥3:1；`text-faint` 只允许用于非关键微标签且仍需 ≥4.5:1。
- `focus-visible`：2px `--text`（墨色）外环、2px 间隔——在无彩系统里墨色焦点环比彩色更醒目。
- 在线、离线、成功、失败永远是"点 + 文字"，不依赖颜色或纹理单独传达。
- 单色图表必须自带图例；图表的 `aria-label` 包含指标名与时间范围；系列靠线宽+图例区分，不靠"看图猜线"。
- 表单错误与保存反馈用 `aria-live="polite"`；错误文案同时说明发生了什么和下一步。
- Dialog 支持焦点圈定、Escape、遮罩点击关闭、关闭后焦点回到触发元素。
- 动效仅限 120–180ms 的颜色/背景过渡与唯一的实时脉冲（2s 呼吸、透明度 0.4–1）；`prefers-reduced-motion` 下全部取消。

## 5. Voice & Tone

- **语域：** 简洁、技术、直接，像印刷在仪器背板上的说明。
- **句式：** 短句，动作优先，不用修辞问句。
- **使用词：** "在线"、"离线"、"保存设置"、"重新加载"、"复制命令"。
- **拒绝词：** "无缝"、"赋能"、"体验升级"、"探索"、"智能洞察"、"一目了然"。
- **称呼：** 不主动称呼用户，控件直接描述动作。
- **微标签：** 结构性小标签用英文大写等宽（`INFRASTRUCTURE`、`ADMINISTRATION`、`LOADING`）——账本的栏目感；面向用户的消息一律中文。
- **失败文案：** 说明发生了什么 + 下一步，例如"主机列表加载失败。检查连接后重试"。

## 6. Implementation Practices

- **Token：** CSS variables 定义在 `:root` 与 `:root[data-theme="dark"]`，经 `@theme inline` 映射到 Tailwind CSS 4（token 名 → `bg-*`/`text-*` utility）。
- **样式分工：** `frontend/src/styles.css` 只放 Tailwind 入口、token、噪点纹理、焦点环等基础设施；组件样式全部用 utility class；不用 CSS Modules、styled-components、静态 inline style。运行时计算值（进度条宽度、canvas 尺寸）可用动态 style。
- **分区网格：** 卡片网格用 `grid gap-4` 留缝；连续细线带（事实带、列表）用容器 `border border-line` + 内层 `grid -mr-px -mb-px`、每行/格 `border-r border-b border-line bg-surface`——末行不满时留白处是干净画布，不露底色。
- **字体与资源：** 不依赖任何外部字体、图片、CDN；噪点纹理必须是内联 data URI；Chart.js 打入生产资源。
- **图表：** `animation: false`、`pointRadius: 0`、`tension: 0`；网格线用 `--border`、刻度用 `--text-muted`、图例用 `--text`；主题切换时重建图表实例以重取 token。
- **动效：** `cubic-bezier(0.2, 0, 0, 1)`，120–180ms；允许的属性只有 color / background-color / border-color / opacity；禁止 transform 位移类动画（按钮 1px 按压位移除外）。

## 7. Anti-Patterns

- **无悬浮圆角卡片海洋。** 卡片是扁平静止的：细线边、微阴影、16px 缝，信息密度不减；拒绝的是"圆角 16 + 阴影 + 图标 + 两行文案 ×6"的营销卡模板，不是卡片本身。
- **chrome 无彩。** 界面骨架（背景、文字、边框、按钮、导航）不出现任何色相；仅两个例外：状态语义色（点、校准线、状态词、错误、危险按钮）与图表数据色。不做"黑白底 + 一个品牌蓝"的折中——折中会同时失去单色的完整性和彩色的表现力。
- **无渐变、光晕、玻璃拟态。** 质感只来自噪点纹理、细线和 1px 边缘光。
- **无药丸标签筛选器。** 筛选项是下划线文本标签；药丸是卡片海洋的同谋。
- **无 emoji、无插画、无图标库。** 产品的信心来自数字准确。
- **无阴影层级体系。** 页面层级仍由细线与字号承担；仅允许两种阴影：容器 4% 透明度微阴影、对话框的克制投影。
- **无 KPI 统计卡行。** 概览的第一信息是"哪台主机什么状态"，不是装饰性的数字摘要。
- **无隐藏状态。** loading、empty、error、offline、unauthorized 都必须有明确设计；空态是排版事件，不是插图事件。
- **无超过 12px 的圆角或 rounded-2xl。** 圆角柔化边缘即可，气泡感会毁掉账本的精确感。

## 8. Decision-Making

1. **可访问性底线不让步。** 单色系统里对比度是唯一的层级工具，任何"更好看"的灰度若跌破 4.5:1 就换灰度，不换标准。
2. **美观优先于密度（用户指令）。** 一屏内两全时都要；冲突时删信息或换页呈现，不牺牲排版质量。但"删信息"指装饰性重复信息，不指主机状态本身。
3. **结构用线，不用块。** 需要分区时先问能不能用一根 1px 线；加阴影或加背景色之前需要理由。
4. **颜色只解释状态和数据。** 状态色标记健康与否，数据色区分图表系列；想让 chrome 里某处"显眼"而想引入颜色时，改用字号、字重或线。
5. **高频路径最短。** 概览→详情的每一步都不能被管理入口、站点描述或装饰拦截。
6. **线性实现。** 同一模式真实复用三次以上才抽象；单色系统的好处是错误显眼，坏了立刻修。

## 9. Workflow

1. 核对现有 API、SSE、OAuth 与安装命令协议，页面结构不变。
2. 重写 `styles.css` 的 token 层：纸/墨双主题中立色阶、图表墨阶、语义色、噪点纹理、焦点环；跑通主题切换。
3. 先做概览账本格（签名组件），在真实数据密度下校准间距与线宽。
4. 再做详情页：细线事实带、等宽区间选择、2×2 墨阶图表与放大对话框。
5. 迁移后台：纵向细线导航、表单分区、虚线凭据区、行带式列表。
6. 全态巡检：loading、empty、error、offline、unauthorized、窄屏（账本格 3→2→1 列）、双主题交叉检查。
7. 反模板检查：对照第 7 节逐条过一遍；无障碍对比度抽查；生产构建确认无外部请求。
